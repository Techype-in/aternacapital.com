# OS junk
.DS_Store
Thumbs.db
desktop.ini
*~

# Editor / IDE
.vscode/
.idea/
*.sublime-*
*.swp
*.swo

# Build / temp
*.bak
*.tmp
*.log
*.cache/

# Environment / secrets — never commit
.env
.env.*
*.env
secrets/
credentials.json
config.local.*

# Node (if added later)
node_modules/
package-lock.json
yarn.lock

# Python (if added later)
__pycache__/
*.pyc
.venv/
venv/

# OneDrive sync junk
*.lnk
~$*

# Local-only design docs (keep spec in docs/ if you want it shared)
*.draft.md
notes-*.md
